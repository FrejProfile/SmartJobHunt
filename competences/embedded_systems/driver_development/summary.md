# Linux driver development

## Learning from the linux kernel
Actively using the linux kernel to learn from, exspecially bootlin, where you can find the specific source files for your device.
By reading the kernel im learning new concepts and patterns fast.

## Creating and interacting with drivers from userspace
Through learning and testing drivers i have interacted with many of the drivers from userspace via their fileoperations.

## Using the device tree for embedded linux
I have used the device tree and its overlays through learning exercises in tutorials. And through writting a custom driver for an arducam board with an ov2640 sensor.

## Experience with communication
Used spi and i2c communication in my arducam driver. I kept them seperated so you can use the i2c part of driver even though you might have a version of a board that doesnt come with spi. 


## Mocking devices with qemu
This is still a skill that im actively developing. But i can compile qemu for different target architectures and test drivers on it.